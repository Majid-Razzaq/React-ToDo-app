TO-DO App
create your project npx create-react-app readt-to-to-app
start your project (npm start)
we removed index.css/app.css file and library from app.js and folder
we clear the code from app.js 
first we include bootstrap in react for this we will search react bootstrap, import the library too of bootstrap class
after all we will use json-server 
start your code in app.js file
after add input field we run this command (json-server --watch db.json --port=3500)